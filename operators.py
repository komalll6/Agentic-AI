#signs to use in math operators
#1. arthmatic operators are- +,-,*,//,/,**
#2. assignment operators are- =,+=,-=,*=,/=,//=,**= to store values in variables
#

example = 20
example += 5 #example = example + 5
print(example) #output will be 25, it will add 5 to the example

a = 10
b = 3
print(a+b)
print(a-b)
print(a*b)

#3. comparison operators are- ==,!=,>,<,>=,<= to compare values, single for assignment, double for comparison
z=10
print(z==10) #output will be True, it will check if z is equal to 10 or not

#4. logical operators are- and, or, not to combine conditional statements
x = 5
y = 10
print(x and y) #output will be 10, it will return the second value if both are true


#5. membership operators are- in, not in to check if a value is present in a sequence or not
#two paraneters- is yaa is-not
#is => agr 2 variable same reference de rhe ho toh output will be True, otherwise False
#not in => agr 2 variable same reference de rhe ho toh output will be False, otherwise True

